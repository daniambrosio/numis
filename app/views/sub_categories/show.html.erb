<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @sub_category.name %>
</p>

<%= link_to 'Edit', edit_category_sub_category_path(:category_id => @category.id, :id => @sub_category.id) %> |
<%= link_to 'Back', category_sub_categories_path(:category_id => @category.id) %>
